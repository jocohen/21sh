TO_DO:

	- Do execve and check if it works with echo
	- Merge it on master !!!
	- Builtin cd (check on minishell and get files pormpt)
	- Replace back_quote (find solution on execve)

Add on:
	when pipe prompt > if no txt redo prompt till txt on it
	Same for cmdand>
	Same for cmdor>
